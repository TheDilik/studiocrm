# --- База данных ---
DATABASE_URL="postgresql://studiocrm:studiocrm@localhost:5432/studiocrm"
# Прямое (непуловое) соединение для миграций. Локально — то же самое;
# на Neon/Vercel — отдельный URL без pgbouncer (DATABASE_URL_UNPOOLED).
DATABASE_URL_UNPOOLED="postgresql://studiocrm:studiocrm@localhost:5432/studiocrm"

# --- NextAuth ---
# Сгенерировать: openssl rand -base64 32
AUTH_SECRET=""

# --- Claude API (AI-помощник) ---
ANTHROPIC_API_KEY=""
AI_MODEL="claude-sonnet-5"
